import { colors } from "./Colors";

const globalStyles = {
    contentPadding: {
        paddingHorizontal: 20,
    },
    fieldInput: {
        borderWidth: 1,
        borderColor: "#ccc",
        padding: 10,
        borderRadius: 5,
        fontSize: 18,
        // fontWeight: "500",
      },

    icons: {
        color: colors.brandColor,
        fontSize: 18,
    }
}

export default globalStyles;